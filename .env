# Pour le moment, on reste sur SQLite pour vérifier que rien ne casse
# DATABASE_URL=sqlite:///instance/chess.db
# Dès qu'on veut basculer sur Docker, on utilisera cette ligne :
DATABASE_URL=postgresql://user:password@localhost:5432/chess_db